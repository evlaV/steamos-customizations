#!/bin/bash

build() {
    @HOLO_BINARIES@

    map add_binary "${HOLO_BINARIES[@]}"

    add_runscript
}

help() {
    cat <<HELPEOF
This hook adds the tools required by the holo custom boot mechanism. Do not
remove this hook unless you know what you're doing.
HELPEOF
}
